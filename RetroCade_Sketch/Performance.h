/*#include <inttypes.h> 
#include <zpuino-types.h>
#include <zpuino.h>
#include "Arduino.h"

typedef struct  {
  boolean splitEnabled;
  byte splitStart;
  byte splitEnd;
  byte splitChannel;
  byte normalChannel; 
} PERFORMANCE_PATCH;

class PERFORMANCE
{
  public:
    void setupPerformance();
    void handleJoystick();
  private:
    PERFORMANCE_PATCH patch[8];
}*/
